- [ ] create a !pair command, accessible only to mods and admins, which makes a round of pairings
- [ ] add a pairings table to say who's paired with whom
- [ ] make the users table more robust with user ids... right now if we've got it running on multiple servers, it will fail
- [ ] stop using a temporary in-memory database and start using a file passed in as a command-line flag or environment variable
- [ ] profit
